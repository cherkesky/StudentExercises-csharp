using System;
using System.Collections.Generic;

class Student {
  public Student(string first, string last, string slack, byte cohort){
    _first = first;
    _last = last;
    _slack = slack;
    _cohort = cohort;
  }
  private string _first;
  private string _last;
  private string _slack;
  private byte _cohort;


}